name: CI

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: ${{ secrets.PG_USER_TEST }}
          POSTGRES_PASSWORD: ${{ secrets.PG_PASSWORD_TEST }}
          POSTGRES_DB: ${{ secrets.PG_DB_TEST }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Prepare test environment
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: ${{ secrets.PG_USER_TEST }}
          DB_PASSWORD: ${{ secrets.PG_PASSWORD_TEST }}
          DB_NAME: ${{ secrets.PG_DB_TEST }}
        run: ./scripts/prepare-test-env.sh

      - name: Create sqlboiler config
        run: |
          cat > sqlboiler.toml << EOF
          [psql]
            host = "localhost"
            port = 5432
            user = "${{ secrets.PG_USER_TEST }}"
            pass = "${{ secrets.PG_PASSWORD_TEST }}"
            dbname = "${{ secrets.PG_DB_TEST }}"
            sslmode = "disable"
          EOF

      - name: Generate SQLBoiler models
        run: make generate

      - name: Build
        run: go build -v ./...

      - name: Run tests
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: ${{ secrets.PG_USER_TEST }}
          DB_PASSWORD: ${{ secrets.PG_PASSWORD_TEST }}
          DB_NAME: ${{ secrets.PG_DB_TEST }}
        run: go test -v ./cmd/... ./internal/config/... ./internal/server/... ./internal/service/...

      - name: Generate test report
        if: always()
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: ${{ secrets.PG_USER_TEST }}
          DB_PASSWORD: ${{ secrets.PG_PASSWORD_TEST }}
          DB_NAME: ${{ secrets.PG_DB_TEST }}
        run: |
          go install github.com/jstemmer/go-junit-report@v2.1.0
          go test -v -json ./cmd/... ./internal/config/... ./internal/server/... ./internal/service/... 2>&1 | go-junit-report > report.xml || true

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-test-report
          path: report.xml